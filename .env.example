# ==================================
# SyteScan Environment Configuration
# ==================================
# Copy this file to .env.local for development
# or .env for production deployments
# ==================================

# ============ Frontend ============
# API endpoint for backend communication
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============ Backend ============
# Database connection string
# Development (SQLite):
DATABASE_URL=sqlite:///./sytescan.db
# Production (PostgreSQL):
# DATABASE_URL=postgresql://user:password@localhost:5432/sytescan

# File upload directory
UPLOAD_DIR=./uploads

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Secret key for security (generate a strong random string for production)
SECRET_KEY=your-secret-key-change-in-production

# CORS allowed origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3000

# ============ Production Only ============
# Uncomment and configure for production

# NODE_ENV=production

# Redis for caching (optional)
# REDIS_URL=redis://localhost:6379

# Maximum file upload size in bytes (10MB default)
# MAX_FILE_SIZE=10485760

# Worker processes
# MAX_WORKERS=4

# Request timeout in seconds
# REQUEST_TIMEOUT=300